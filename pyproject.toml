[project]
name = "fsuk-bom-uploader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "selenium>=4.44.0",
    "typer>=0.26.7",
]

[tool.uv]
package = true

[project.scripts]
uploader = "uploader.cli:app"